<%= form_with(model: publication) do |form| %>
  <% if publication.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(publication.errors.count, "error") %> prohibited this publication from being saved:</h2>

      <ul>
        <% publication.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :publication_type, style: "display: block" %>
    <%= form.select :publication_type, @publication_types, {}, {value: params[:publication_type], id: :publication_publication_type} %>
  </div>

  <div>
    <%= form.submit "Update", class: "btn btn-primary" %>
  </div>
<% end %>
