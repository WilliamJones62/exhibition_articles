<%= form_with(model: exhibition) do |form| %>
  <% if exhibition.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(exhibition.errors.count, "error") %> prohibited this exhibition from being saved:</h2>

      <ul>
        <% exhibition.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :year, style: "display: block" %>
    <%= form.number_field :year, readonly: true %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
